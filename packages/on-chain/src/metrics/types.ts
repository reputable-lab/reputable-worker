export type FetchBlockState = {
  totalBlocks: number;
  currentBlock: number; //block where we are but have not fetched yet
};

export type ProcessBlockState = {
  totalBlocks: number;
  currentBlock: number; //block where we are but have not fetched yet
};
